# AI 魔法修图 (Magic Lens) - 产品需求与技术架构文档

## 一、 产品愿景 (Product Vision)
一款专为女性用户设计的“直觉修图”工具。用户无需掌握复杂的修图参数，只需通过对话（如“把天空调成粉色”、“去路人”、“ccd滤镜”）即可完成高质量的照片后期。

*   **核心痛点**：传统修图软件（PS, 美图秀秀）操作繁琐、参数过多；旅游/生活照精修耗时。
*   **目标平台**：首发网页版 (Next.js)，后续适配微信小程序及 iOS。

## 二、 交互逻辑与 UI 设计 (UI/UX Design)

界面采取**双栏布局**，强调“所见即所得”的修改过程。

### 1. 左侧：核心画布区 (Canvas Area)
*   **多图历史轮播 (Image Stack Carousel)**：
    *   左侧区域中心展示当前选中的图片。
    *   底部设有一个横向滚动的缩略图列表。
    *   **最左侧第一张**永远是用户的**原始图 (Origin)**。
    *   每生成一张新图，自动追加到列表末尾。
    *   **点击切换**：点击任意缩略图，大图区域同步切换。后续指令将基于当前选中的图片进行迭代。
*   **功能按钮组**：
    *   **原图对比 (Compare)**：位于大图下方左侧。长按或点击后，通过透明度渐变或滑动条展示“当前图 vs 原始图”的对比效果。
    *   **“就这张了” (Finish)**：点击后保存当前选中的图片到本地，并弹出确认提示：“已保存，是否开启下一张？”，确认后清空历史记录，保留一个空白上传状态。

### 2. 右侧：交互控制区 (Interaction Area)
*   **顶部：弹性预设词 (Preset Chips)**：
    *   采用胶囊状按钮 (Chips)，横向滚动展示。
    *   支持动态扩展分类。
    *   **交互**：点击预设词（如“CCD质感”），文本自动填充至下方输入框，方便用户二次修改。
*   **中部：对话流 (Chat Stream)**：
    *   展示简单的气泡对话。
    *   展示 AI 的处理状态提示（如“正在分析光影...”、“正在渲染滤镜...”）。
*   **底部：输入与上传 (Input & Upload)**：
    *   **左侧上传**：支持点击和拖拽图片。
    *   **中间输入**：支持回车发送。
    *   **右侧发送**：点击后将“当前选中图 + 提示词”发送给后端。

## 三、 功能逻辑细节 (Functional logic)

### 1. 迭代修图逻辑 (Iterative Editing)
*   **状态继承**：当用户选中“历史记录2”并输入新指令时，系统会将“历史记录2”作为 Base Image 发送给 AI。
*   **色系保持与审美优化**：
    *   **内置 System Prompt**：在发送给 AI 的请求中，系统会自动注入一段隐含的约束词，以确保结果符合目标用户的审美。
    *   **审美准则**：保持五官结构、皮肤冷白透亮、画面色彩饱满但不过曝、保留自然纹理。

### 2. 预设提示词库 (Preset Library)
*   初期内置包含：CCD滤镜、氛围感雪景、冷白皮磨皮、瘦脸瘦身等。
*   配置项存储在独立文件，支持快速更新。

## 四、 技术架构 (Technical Architecture)

### 1. 技术栈 (Tech Stack)
*   **框架**：Next.js 15 (App Router) + React 19。
*   **UI/组件**：Tailwind CSS + shadcn/ui (Radix UI)。
*   **动画**：Framer Motion (用于对比、切换、加载状态)。
*   **状态管理**：Zustand (管理图片历史栈、当前选中索引)。
*   **部署**：Vercel。

### 2. 配置管理 (Configuration Policy)
*   **严禁硬编码**：所有 API Key、中转地址、模型名称、预设词列表必须放在 `.env.local` 或 `src/config/app.config.ts`。
*   **API 接入**：
    *   **地址**：`https://models.kapon.cloud/v1`
    *   **模型**：`gemini-3-pro-image-preview`
    *   **协议**：OpenAI 兼容协议。

### 3. 数据流设计 (Data Flow)
1.  用户上传 -> 前端转 Base64 -> 存入 Zustand `imageStack`。
2.  用户输入 -> 触发 API 请求 -> 携带 `imageStack[currentIndex]` + `userPrompt` + `systemPrompt`。
3.  后端响应 -> 返回新图 URL 或 Base64 -> 前端追加至 `imageStack` -> 自动跳转至新图索引。
